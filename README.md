# Coding Agent Pipeline Test
This is a small Python project for testing a coding agent.
The project simulates a simple data-processing pipeline. It receives batches of numbers, processes them, and calculates basic statistics.
The pipeline can fail when an empty batch is received because the average calculation divides by the number of values.
